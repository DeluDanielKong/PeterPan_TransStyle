# PeterPan_TransStyle


corpus file contains the original corpus data, which is used for training and testing the model. However, due to copyright issues, we only present a small sample of the corpus data in the repository. If you want to access the full corpus data, please contact us directly.

Feature_set.xlsx contains full feature set, feature selection data, and salient features.